/**
 * titles.ts — Tipos para o sistema de títulos com desbloqueio automático.
 *
 * FASE 3: reutiliza MissionConditions / MissionRule de missions.ts — sem duplicação.
 */

export type { MissionConditions, MissionRule, MissionRuleType } from "@/types/missions";
export type { TitleRarity, DbTitle, DbUserTitle, TitleInput } from "@/lib/titleService";
