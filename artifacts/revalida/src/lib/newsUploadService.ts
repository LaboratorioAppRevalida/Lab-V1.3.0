/**
 * @deprecated Use `uploadFile` de `@/lib/storageService` diretamente.
 * Este arquivo existe apenas para evitar imports quebrados durante a transição.
 */
export { uploadFile as uploadNewsImage } from "./storageService";
